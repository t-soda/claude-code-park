// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { ActivityState } from "./ActivityState";
import type { SessionStatus } from "./SessionStatus";
import type { SubAgentRun } from "./SubAgentRun";

/**
 * The main Claude Code session.
 */
export type Session = { session_id: string, 
/**
 * Working directory (cwd). Taken from the JSONL cwd field.
 */
project: string, git_branch: string | null, slug: string | null, status: SessionStatus, started_at: string | null, last_event_at: string | null, current: ActivityState, 
/**
 * Whether this is a main session not tied to an agent definition. Always true (a main session is never tied to an agent definition).
 */
is_main: boolean, 
/**
 * Sub agents launched from this session.
 */
subagents: Array<SubAgentRun>, };
